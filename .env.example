# Copy to .env and fill in. Never commit .env. Never put a real value in this file.

APP_ENV=
LOG_LEVEL=
API_PORT=

# Database (PostgreSQL 15 with psycopg3)
# Inside Docker: postgresql+psycopg://argus:argus@db:5432/argus
# On host machine: postgresql+psycopg://argus:argus@localhost:5432/argus
DATABASE_URL=
DB_STATEMENT_TIMEOUT_MS=

# Secrets (required, read from environment only, no defaults)
GITHUB_TOKEN=
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=

# Inference and Runtime (Ollama on host, DEC-017)
# Inside Docker: http://host.docker.internal:11434
# On host machine: http://localhost:11434
OLLAMA_URL=
MODEL_ID=
OLLAMA_SEED=
OLLAMA_NUM_PREDICT=
PROMPT_VERSION=

# Reliability & Networking
HTTP_TIMEOUT_SECONDS=
HTTP_MAX_ATTEMPTS=

# Thresholds (DATA_AND_EVIDENCE.md 6.8)
FRESHNESS_WINDOW_HOURS=
RECENT_ACTIVITY_DAYS=
PR_REVIEW_WAIT_DAYS=
DRAFT_PR_STALE_DAYS=
ISSUE_NO_CODE_DAYS=
DUE_SOON_DAYS=
STATUS_STUCK_DAYS=
NO_ACTIVITY_DAYS=
EXCERPT_MAX_CHARS=
MAX_EVIDENCE_ITEMS=
SYNC_INTERVAL_MINUTES=

# Freshness display warnings
FRESHNESS_WARN_MINUTES=
FRESHNESS_STALE_MINUTES=
